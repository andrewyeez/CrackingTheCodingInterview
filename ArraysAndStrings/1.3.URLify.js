// write a method that replaces all the spaces in a string with "%20"
// assume the string as sufficient space to add %20 into
// assume we are given the correct string length
// inputs would be the string and the string length

function URLify(str, length){
  // iterate over the string w/ str.charAt
  // when we encounter the space, insert the characters "%20" starting from
  // position of the space. replace the space with the "%20" characters and move
  // the existing non space character to the next index
}
